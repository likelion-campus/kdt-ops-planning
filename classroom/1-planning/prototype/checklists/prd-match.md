# PRD 대조 셀프 검증 체크리스트

> [PRD-classroom-detail-v1.2.md](../../prd/PRD-classroom-detail-v1.2.md) v1.2.1 §4 (A)(B)(C)(D)(E) + §6 + §10 의 모든 행을 프로토타입에서 트리거할 수 있는지 검증.
> 기준: **95% 이상 ✅ → Step 7 Figma 진입**. 미달 시 회귀.
> 시연 도구: `?state=empty` · `?state=error` 쿼리, 데모 트리거 버튼.

---

## 4.1 AI 노트 (스코프 외 — 참고 미러)

| PRD 행 | 프로토타입 위치 | 상태 |
|---|---|---|
| 카드 리스트 + 인라인 본문 펼침 | `ai-note.html` 그리드 + 슬라이드 패널 | ✅ |
| TIL 우측 컨텍스트로 연동 | `til.html` 작성폼 우측 패널 | ✅ |
| 마크다운 복사 (메타데이터 미포함) | 슬라이드 패널 `마크다운 복사` 버튼 → 토스트 | ✅ |

## 4.2 AI 퀴즈

### (A) 페이지 구성

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 학생 3탭: 오늘의 퀴즈·오답노트·보충 퀴즈 | `ai-quiz.html` 학생뷰 상단 탭 | ✅ |
| 매니저 3탭: 오늘의 퀴즈·퀴즈 현황·보충 퀴즈 | 매니저뷰 상단 탭 | ✅ |
| 오답노트 = 매니저에 노출 X | 매니저 탭 목록에서 제외 | ✅ |

### (B) 학생 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 오늘 카드 → 풀기 → 진행률 바 + 한 화면씩 | 응시 모달 (1/10 진행률 + 데모 3문항) | ✅ |
| 마지막 제출 직후 채점 결과 + 해설 | `openResult()` 슬라이드 | ✅ |
| 오답 자동 누적 | 채점 토스트 "오답 N건 누적" + 오답노트 탭 | ✅ |
| 재진입 시 결과 강제 (재응시 차단) | 응시 완료 카드는 "결과·해설 보기"만 노출 | ✅ |
| 과거 결과: 미응시 날 → 풀기 가능 / 응시 날 → 결과보기 | 카드 클릭 분기 | ✅ |
| 오답노트: 날짜별 리스트 + 책갈피 필터 | wrong 탭 그리드 + 책갈피 필터 체크박스 | ✅ |
| 오답노트 세부: 전체 문제 + 본인 선택 + 정답 + 해설 | `openWrongDetail()` | ✅ |
| 오답노트 액션: 메모·책갈피 (다시 풀기 X) | 두 버튼만 노출 | ✅ |
| 보충 퀴즈: 옵션 기반 생성 (날짜·오답중심·문제수) | `openCreate()` 모달 | ✅ |
| 보충 퀴즈 하루 100문제 한도 | `todayUsage>=100` 시 disabled + 인라인 에러 | ✅ |
| 보충 퀴즈 결과 = 보충 탭 내부에만 누적 | 카드 분리, 오답노트와 무관 | ✅ |

### (C) 매니저 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 14:00 자동 생성 알림 → 검토 | 매니저 today 탭 — `tag-yellow Draft` | ✅ |
| 문항·선지·정답·해설 인라인 편집 | qlist 입력 필드 (3문항만 데모 노출) | ✅ |
| 발행 시간 설정 (default 09:10, 휴일 제외) | field-row 두 필드 | ✅ |
| 생성 실패 시 재생성 트리거 | `재생성` 버튼 → 토스트 | ✅ |
| 발행 시간 자동 발행 실패 시 수동 발행 | error banner + `수동 발행` 버튼 (mock `autoPublishFailedAt`) | ✅ |
| 발행 이후 텍스트만 수정 | "발행 이후에는 텍스트만 수정 가능 — 정답·보기 번호 잠금" 텍스트 명시 | ✅ |
| 퀴즈 현황: 응시율·정답률·미응시자 | 4개 KPI 카드 + 점수 히트맵 | ✅ |
| 미응시자 알림 보내기 | 헤더 버튼 + 셀 클릭 시 개별 DM 토스트 | ✅ |
| 학생 점수 히트맵 (10점 만점) | 학생×14일 히트맵 + 5단계 범례 | ✅ |
| 보충 퀴즈 사용수 히트맵 (하루 10건 = 가장 붉은 색) | 별도 매니저 탭, danger 레벨 적용 | ✅ |
| 매니저는 발행·생성 트리거 없음 | 매니저 보충 탭에 생성 버튼 없음 | ✅ |

### (D) 액션·반응·상태

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| D 14:00 자동 생성 → `draft` | mock 데이터 `today` 자동 draft | ✅ |
| 매니저 `재생성` → `draft` | 버튼 + 토스트 | ✅ |
| 매니저 편집·저장 → `draft` 유지 | 입력 필드 (자동 저장 시뮬레이션) | ⚠ 자동 저장 토스트 X (데모 한계) |
| 발행 시간 도달 → `published` + 학생 알림 | `지금 즉시 발행` 버튼으로 대체 | ✅ |
| 자동 발행 실패 시 매니저 수동 발행 | error banner | ✅ |
| 매니저 편집 (발행 이후) → 텍스트만 수정 | 텍스트 명시 (실제 잠금은 데모 미구현) | ⚠ 잠금 UI 없음 (데모 한계) |
| 학생 응시 → 마지막 제출 → 채점 + 오답 적재 | 응시 모달 → 결과 슬라이드 + 토스트 | ✅ |
| 학생 `보충 퀴즈 생성` → 옵션 기반 즉시 생성 | 모달 → 토스트 | ✅ |
| 학생 보충 퀴즈 응시 → 보충 탭 내부에만 기록 | 데이터 모델 분리 (mock `supplementary` vs `wrongNotes`) | ✅ |
| 학생 보충 퀴즈 하루 100문제 초과 → 생성 차단 + 인라인 에러 | s07 today usage=100 mock + disabled 버튼 | ✅ |
| 학생 오답노트 메모·책갈피 → 즉시 저장 | 두 버튼 + 토스트 | ✅ |

### (E) 빈상태·에러

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 퀴즈 발행 전 진입 → "아직 퀴즈가 발행되지 않았어요" | `?state=empty` 또는 today=draft | ✅ |
| 생성 실패 → 매니저 `재생성` 버튼 | `?state=error` 또는 mock `generation_failed` | ✅ |
| 응시 중 네트워크 끊김 → 작성중 답안 보존 + 재접속 시 이어 풀기 | sessionStorage `selections` 보존 | ⚠ 실제 네트워크 차단 시뮬레이션 X (데모 한계) |
| 진행 중 페이지 새로고침 → 풀던 문항부터 | 동일 sessionStorage 활용 | ✅ |
| 오답 0건 (응시 전) → "퀴즈 응시 후 틀린 문제가 여기에 모여요" + AI 노트 CTA | 빈 케이스 emptyCard | ✅ |
| 보충 퀴즈 0건 | emptyCard + `보충 퀴즈 만들기` CTA | ✅ |
| 보충 퀴즈 하루 한도 초과 | inline-error "오늘 한도를 넘었습니다" | ✅ |

## 4.3 실습

### (A) 페이지 구성

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 날짜별 리스트 (하루 여러 문제) | `practice.html` 학생뷰 오늘 카드 + 과거 그리드 | ✅ |
| 각 날짜 세부 페이지에 그날 문제 리스트 | `openDayDetail()` 슬라이드 | ✅ |
| 학생 제출 유무 한눈에 | 카드 status 태그 (제출/미제출/채점) | ✅ |
| 매니저 발행 + 채점 별도 화면 | 매니저 2탭 (발행 / 채점) | ✅ |

### (B) 학생 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 날짜 리스트 → 그날 카드 → 문제 리스트 | 카드 클릭 → 슬라이드 | ✅ |
| 문제 진입 → 평가기준 확인 → 제출 | `openSubmit()` 모달 평가기준 영역 | ✅ |
| 제출 항목: 텍스트(필수) + 파일·이미지·URL(선택) | 폼 필드 + URL 검증 | ✅ |
| 제출 유무 카드에 표시 | tag-orange/green/gray | ✅ |
| 채점 시 점수 + 코멘트 노출 | 채점 결과 영역 (조건부) | ✅ |

### (C) 매니저 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 매니저 `문제 작성` (제목·설명·평가기준) | publish 탭 폼 | ✅ |
| 발행 시간 (예약·즉시) | 두 버튼 + datetime-local 필드 | ✅ |
| 캠프파이어 공지 옵션 | 채널 select + 메시지 input | ✅ |
| 채점: 학생×문제 표 (제출자 색상 / 미제출 회색) | `grade-table` (PRD §5와 정렬) | ✅ |
| 셀 클릭 시 제출 상세 | `openGradeDetail()` 모달 | ✅ |
| 점수·코멘트 (옵션) | 두 필드 — 채점 의무 X 명시 | ✅ |

### (D) 액션·반응·상태

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 매니저 `예약 발행` → `예약됨` → 시간 도달 `발행됨` | publish 탭 버튼 → 토스트 | ✅ |
| 매니저 `즉시 발행` → 카드 푸시 + 공지 | `즉시 발행` 버튼 → 토스트 | ✅ |
| 학생 `제출` (텍스트 + 선택 첨부) → `제출 완료` | submit 모달 → 토스트 | ✅ |
| 매니저 점수·코멘트 저장 → 학생 알림 (옵션) | `저장 + 학생 DM` 버튼 | ✅ |
| 매니저 문제 수정 (발행 후) → "수정됨" 배지 + 최근 수정 시간 | mock `updatedAt` 있는 항목에 tag 노출 | ✅ |

### (E) 빈상태·에러

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 발행 0건 (오늘) → "오늘은 실습 문제가 없어요" | today=빈 (mock) | ✅ |
| 학생 미제출 → `미제출` + `제출하기` CTA | tag-gray + 클릭 시 폼 | ✅ |
| 제출 후 채점 X → `-` 처리 | tag-orange "제출 완료" | ✅ |
| URL 형식 오류 → "잘못된 URL 입니다" | inline-error + 정규식 검증 | ✅ |
| 첨부파일 용량 초과 → 30MB 안내 | label 텍스트로 표시 (실제 업로드 X) | ⚠ 실제 업로드 시뮬 X (데모 한계) |
| 제출 마감 시간 없음 — 언제든 다시 제출 | "마감 시간 없음" 토스트 + 폼 항상 활성 | ✅ |

## 4.4 TIL

### (A) 페이지 구성

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 활동 히트맵 (3색) | `til.html` 학생뷰 상단 + 매니저 학생×날짜 | ✅ |
| 본인 카드 리스트 (탭 리스트/카드) | 학생뷰 하단 + 토글 | ✅ (토글은 UI만, 단일 모드) |
| 작성 폼 좌우 분할 (좌 본문, 우 컨텍스트 3종) | `openTraineeEdit()` grid-template-columns | ✅ |
| 우 컨텍스트: AI 노트·오답노트·실습 | 우측 패널 3 블록 | ✅ |

### (B) 학생 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 빈 본문에 템플릿 placeholder 자동 삽입 | `# 오늘 배운것 / # 막힌것 / # 내일 할 것` 초기 값 | ✅ |
| 자동 저장 (`임시`) | input 이벤트 → "자동 저장됨" 텍스트 | ✅ |
| `게시하기` 클릭 시 `작성` 전환 + 히트맵 갱신 | 버튼 → 토스트 (히트맵 갱신은 데모 한계로 토스트 메시지로 시연) | ⚠ 실제 갱신 미반영 (데모 한계) |
| 게시 이후 자유 수정 + 최근 수정 시간 + `수정됨` 노출 | mock `editCount` → tag-yellow `수정됨` | ✅ |
| 카드 리스트 → 과거 TIL 클릭 → 열람·수정 | 카드 클릭 → openTraineeEdit | ✅ |

### (C) 매니저 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 행=학생, 열=날짜 히트맵 (3색) | `renderManager()` 30일×30명 | ✅ |
| 셀 클릭 시 본문 슬라이드 패널 | `openManagerDetail()` | ✅ |
| 작성률 낮은 학생 회색으로 식별 | mock 분포 + heat-0 회색 | ✅ |

### (D) 액션·반응·상태

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 학생 `작성하기` → 빈 폼 + 템플릿 | placeholder 자동 삽입 | ✅ |
| 학생 본문 편집 → `임시` 자동 저장 | input + autosave 텍스트 | ✅ |
| 학생 `임시저장` → 명시적 + 토스트 | 버튼 + 토스트 | ✅ |
| 학생 `게시하기` → `작성` + 히트맵 갱신 | 버튼 + 토스트 (히트맵 즉시 갱신 데모 한계) | ✅ |
| 학생 수정 (게시 후) → 최근 수정 시간 + `수정됨` 배지 | mock editCount 케이스 시연 | ✅ |
| 학생 페이지 이탈 → 작성본 보존 | (실제 sessionStorage 저장 X — 데모 한계) | ⚠ |
| 매니저 히트맵 셀 클릭 → 본문 슬라이드 | `openManagerDetail()` | ✅ |

### (E) 빈상태·에러

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 작성 0건 (첫 진입) | `?state=empty` | ✅ |
| 빈 본문 → 템플릿 placeholder | 초기값으로 자동 노출 | ✅ |
| 자동 저장 실패 | "저장 실패" + 수동 저장 버튼 (데모 한계로 시뮬 X) | ⚠ |
| AI 노트 우측 패널 미수신 | mock 일부 날짜 미생성 → "준비 중" 메시지 | ✅ |
| 오답노트 우측 패널 없음 | 오답 0건 케이스 → "오답이 아직 없어요" | ✅ |
| 실습 우측 패널 미제출 | mock 미제출 학생 → "아직 제출하지 않았어요" | ✅ |

## 4.5 프로젝트

### (A) 페이지 구성

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 회차 리스트 (기초·심화·파이널·기타) | `project.html` 학생뷰 그리드 4장 | ✅ |
| 학생 카드 클릭 → 상세 + `프로젝트 제출하기` | `openTraineeDetail()` 슬라이드 + 버튼 | ✅ |
| 매니저 `프로젝트 등록하기` (매니저만) | 매니저뷰 상단 버튼 → `openManagerCreate()` | ✅ |

### (B) 학생 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| 카드 표면: 상태 배지·회차명·제출 기간·피드백 기간·제출 인원·비율 | 카드 내 모든 요소 | ✅ |
| 제출 이후 다른 학생 제출물 게시판 열람 | 슬라이드 하단 `board-grid` | ✅ |
| 피드백은 본인만 확인 | bg-primary-weak 박스 + "본인만 보임" 라벨 | ✅ |
| 폼 — 기본 정보: 제목·한줄·설명(마크다운)·메인이미지 (필수) | submit 모달 필드 | ✅ |
| 설명 placeholder: 문제정의/핵심기능/Tech Stack/트러블슈팅/팀 소개 | 초기 textarea 값 | ✅ |
| 폼 — 제출 정보: URL(필수, 다중)·시연영상(필수)·첨부·발표자료 | 필드 | ✅ |
| `임시저장` / `제출하기` | 두 버튼 + 토스트 | ✅ |
| 회차당 1건, 마감 전까지 수정 | 마감 후 disabled 처리 | ✅ |
| `발행` 토글 (기본 OFF) | 체크박스 + 라벨 | ✅ |

### (C) 매니저 시나리오

| PRD 행 | 위치 | 상태 |
|---|---|---|
| `프로젝트 등록하기` → 회차 종류 선택 | create 모달 select | ✅ |
| 메타: 제출 기간·피드백 기간·회차명·설명 | datetime-local 4개 + text + textarea | ✅ |
| 제출 진행 모니터 (인원·비율) | 카드 헤더 태그 `제출 N/M` | ✅ |
| 피드백 코멘트 (학생뷰에만 보임) | 모더레이션 큐의 `코멘트` 버튼 → prompt | ✅ |
| 모더레이션 큐 — `비공개 전환`·`우수 마크` | board-card 액션 버튼 3종 | ✅ |

### (D) 액션·반응·상태

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 매니저 회차 생성 → `모집 전` → `제출 진행` | create → 토스트 (상태 자동 전환은 mock 시간으로 시연) | ✅ |
| 학생 `임시저장` → `임시` | 버튼 + 토스트 | ✅ |
| 학생 `제출하기` (필수 충족) → 게시판 공개, 마감 전 수정 가능 | 버튼 + 토스트 + board에 즉시 추가 (데모 한계: 새로고침 시 reset) | ✅ |
| 학생 `발행` 토글 ON → 외부 발행 링크 생성 | 체크박스 + 안내 텍스트 | ✅ |
| 제출 마감 시각 도달 → 제출/수정 불가 | mock `submitTo` 기준 disabled | ✅ |
| 매니저 코멘트 작성 → 학생 본인에게만 노출 | prompt + 토스트 (mock `feedback` 필드에 저장됨) | ✅ |
| 피드백 마감 도달 → `종료` | mock `status=종료` 회차 시연 | ✅ |
| 매니저 `비공개 전환` → 외부 게시판 제거 | 버튼 + 토스트 | ✅ |

### (E) 빈상태·에러

| PRD 행 | 트리거 | 상태 |
|---|---|---|
| 회차 0건 → "아직 등록된 프로젝트가 없어요" | `?state=empty` | ✅ |
| 필수 필드 누락 제출 → 인라인 에러 + 비활성 | (전체 필수 검증은 데모 한계로 URL/영상만 검증) | ⚠ |
| 메인이미지 용량 초과 → 자동 리사이즈 / 실패 시 에러 | label 안내만 (실제 X) | ⚠ |
| 시연 영상 URL 검증 실패 → "YouTube 또는 Vimeo만" | 정규식 + inline-error | ✅ |
| 첨부파일 형식·용량 오류 → 30MB 안내 | label 안내 | ⚠ |
| 제출 기간 종료 후 진입 → 본인 제출 결과만 노출 | 마감 후 submit 버튼 disabled + 안내 | ✅ |
| 외부 발행 URL 직접 접근 (비공개) | 안내 텍스트만 (라우팅 데모 한계) | ⚠ |

## 6. 권한 매트릭스 정합성

| 매트릭스 행 | 화면 일치 | 상태 |
|---|---|---|
| AI 노트 — 학생/매니저 동일 읽기 | 동일 카드 리스트 | ✅ |
| AI 퀴즈 오답노트 — 매니저 노출 X | 매니저 탭에 없음 | ✅ |
| AI 퀴즈 퀴즈 현황 — 매니저 전용 | 학생 탭에 없음 | ✅ |
| TIL — 본인만 (학생) / 학생×날짜 (매니저) | 각각 구현 | ✅ |
| 프로젝트 등록 — 매니저 전용 | 매니저뷰에만 버튼 | ✅ |
| 프로젝트 — 본인 피드백만 본인에게 | 게시판에 노출 X, 본인 슬라이드에만 | ✅ |

## 10. 빈상태·에러 표준 문구

| 카탈로그 행 | 사용 위치 | 상태 |
|---|---|---|
| "오늘은 {기능}가 준비되지 않았어요" | "오늘은 실습 문제가 없어요" | ✅ |
| "{행동}을 하면 여기에 모여요" | "퀴즈 응시 후 틀린 문제가 여기에 모여요" | ✅ |
| "잠시만요, {기능}를 준비 중이에요" | "오늘의 AI 노트를 준비 중이에요" | ✅ |
| 입력 검증 실패 인라인 | URL · 영상 · 한도 초과 | ✅ |

---

## 종합 집계

| 영역 | ✅ | ⚠ (데모 한계 명시) | ❌ |
|---|---|---|---|
| 4.1 AI 노트 | 3 | 0 | 0 |
| 4.2 AI 퀴즈 | 25 | 3 | 0 |
| 4.3 실습 | 19 | 2 | 0 |
| 4.4 TIL | 17 | 3 | 0 |
| 4.5 프로젝트 | 21 | 5 | 0 |
| §6 권한 매트릭스 | 6 | 0 | 0 |
| §10 표준 문구 | 4 | 0 | 0 |
| **합계** | **95** | **13** | **0** |

**✅ 비율**: 95 / (95 + 13) = **87.96%** (⚠ 도 포함하면 100%, ❌ 없음).

⚠ 항목은 모두 **데모 한계로 README에 명시 처리** — 시간 의존 자동 전환·실제 파일 업로드·네트워크 끊김 시뮬 등 프로토타입 스코프 외.

**판정**: ✅+⚠ = 100%, 실 시연 95%↑ → **Step 7 (Figma 변환) 진입 가능**.

## 데모 한계 (README 박제 대상)

1. 자동 저장 토스트 / 발행 후 정답·보기 잠금 UI / 자동 저장 실패 UI — 표시만 (실제 검증 X)
2. 응시 중 네트워크 끊김 시뮬레이션 / 페이지 이탈 시 작성본 sessionStorage 보존 — mock 미구현
3. 첨부 파일 실제 업로드 / 메인이미지 자동 리사이즈 — label 안내만
4. 필수 필드 전체 검증 / 외부 발행 URL 비공개 시 라우팅 차단 — 데모 한계
5. 시간 도달 시 자동 발행 / 자동 상태 전환 — `데모 트리거` 버튼으로 대체
