---
id: POL-CR-01
title: "클래스룸 핵심 정책 v1.8 — 권한 · AI 퀴즈 · 실습 · TIL · 프로젝트 · 알림 · 첨부"
owner: "@pm-classroom-lead"
status: draft
version: 1.8
date: 2026-06-16
related_prd: [PRD-classroom-detail-v1.2]
supersedes: POL-CR-01-classroom-core-v1.2 (snapshot: policies/archive/POL-CR-01-classroom-core-v1.2-snapshot-2026-06-09.md)
terms: [Trainee, Manager, Quiz, WrongNote, SupplementaryQuiz, Practice, TIL, Project, Notification]
---

# POL-CR-01 — 클래스룸 핵심 정책 (v1.8)

> **단일 진실**: 본 정책 파일은 `PRD-classroom-detail-v1.2` §1·§6·§8·§9에서 합의된 항목만 추려 정리한다.
> 시나리오·페이지 구성·UI 패턴은 PRD 본문(§2~§5, §10)에서 본다.
> 구현 디테일(스케줄러·이벤트 키·DB)은 `classroom/3-backend/` 개발 spec으로 위임한다.

---

## 0. 의도

학생은 **"하루 일과 학습판"**을, 매니저는 **"현황 진단판"**을 갖는다.
정책은 **합의가 끝난 규칙**만 담는다. 합의 진행 중 항목은 PRD `§11 변경 이력`에서 추적한다.

---

## 1. 권한 (클래스룸 내 학생 / 매니저 2종)

> **범위 주의**: 본 권한은 **클래스룸 도메인 한정**이다.
> 캠프파이어 · Admin 도메인은 별도로 **4종 권한(학생 · 강사 · 퍼실 · 매니저)**으로 분리 운영한다 — 채널·운영 정책이 다르기 때문.

1. **클래스룸 내 권한은 학생 / 매니저 2종**으로만 운영한다.
2. **클래스룸의 매니저 권한 1종**을 강사 · 퍼실 · 운영담당자가 동일하게 공유한다 (역할별 분리 권한은 두지 않음).
3. **캠프파이어 · Admin 권한 = 학생 / 강사 / 퍼실 / 매니저 4종**으로 별도 운영한다. 클래스룸의 "매니저"는 캠프파이어·Admin 상에서는 본인의 직책(강사·퍼실·매니저) 권한으로 분리되어 동작한다.
4. 분반·강의별 필터링은 권한이 아닌 사용자별 자체 적용으로 처리한다.
5. **권한 = 데이터 접근 범위**, **시나리오 = 메뉴별 실제 동작**으로 분리해 정의한다 (메뉴별 매트릭스는 PRD §6).
6. 클래스룸 LNB는 학생·매니저 공통 고정 구조를 사용하며, 우상단 토글로 학생뷰 / 매니저뷰를 전환한다.

---

## 2. AI 퀴즈 — 자동 발행

1. 문항은 **객관식 10문제**만 발행한다. OX·단답형은 도입하지 않는다.
2. AI 노트 기반 Draft는 **전일(D) 14:00 자동 생성**한다.
3. 학생 발행 시각의 **default 는 09:10**, 매니저가 회차별로 변경할 수 있다.
4. **휴일은 자동 발행하지 않으며, 다음 수업일로 자동 이월**한다.
5. **00시 기준 하루 1회만 자동 발행**한다. 자동 재시도는 하지 않는다.
6. 자동 발행 실패 시 **매니저가 수동으로 발행**할 수 있다.
7. Draft 단계에서는 매니저가 문항·선지·정답·해설을 자유 편집하고, **재생성 트리거**를 사용할 수 있다.
8. **발행 이후에는 텍스트(문항·해설)만 수정 가능**하며, **정답·보기 번호는 잠금**된다.
9. 학생 응시는 **1회만 허용**한다. 재진입 시 즉시 결과 페이지를 노출한다.
10. **학생뷰에는 소요시간을 일절 표시하지 않는다** (v1.4) — 응시 중·결과 화면·과거 풀이 내역·보충 퀴즈 결과 모두 비표시. 소요시간 집계는 **매니저뷰 진단 전용**으로만 사용한다.
11. 매니저 발행 폼의 **`임시저장` · `예약발행` · `즉시발행` 세 액션 모두 빈 칸이 있으면 실행되지 않는다** (v1.4) — 빈 필드(퀴즈 제목·문제·보기·정답·해설) 인라인 에러("아직 입력하지 않은 칸이 있어요. 모든 칸을 채워주세요") 후 차단.
12. 매니저는 **발행본 상세에서 `퀴즈 풀이`(수업용)로 학생과 함께 한 문제씩 풀 수 있다** (v1.5) — 보기 선택 후 정답·해설을 즉시 공개하며 문항을 넘긴다. **채점·집계·오답노트 누적에는 반영하지 않으며**(진행용), 매니저뷰 전용이다.
13. **매니저뷰 발행본 상세(결과 요약·학생별 점수·미응시 명단)와 발행 퀴즈 리스트의 인원 집계는 `퀴즈 생성 시점` 기준 재적생만 대상으로 한다** (v1.7) — 전체 학생수(분모)·응시율·평균·미응시 명단은 **그 퀴즈 생성일 기준 중도포기자를 제외한 재적생**이 기준이다. **생성 이후 중도포기자는 응시 점수를 보존**해 그대로 노출하고(`중도포기` 배지로만 식별, 흐림·잠금 없음), **생성 시점에 이미 중도포기한 학생은 발행본 상세·리스트 집계에서 제외**한다. 발행본 상세 상단에 `퀴즈 생성 시점 기준 재적생` 안내를 노출하고, **중도포기자 현황은 출결현황에서 확인**하도록 가이드한다. *(보충 퀴즈 사용현황 히트맵은 특정 퀴즈 시점이 없는 누적 뷰이므로 본 규칙에서 제외한다.)*

## 3. AI 퀴즈 — 오답노트 · 보충 퀴즈

### 3.1 오답노트

1. **오답노트는 별도 메뉴를 두지 않고 `AI 퀴즈` 메뉴 안 탭으로만 존재**한다.
2. **오답 자가 등록은 불가**하다. 오답은 퀴즈 응시 결과로만 자동 누적된다.
3. 오답노트는 **날짜별 문제 리스트**로 표시한다. 카드 클릭 시 그날 틀린 문제 세부 화면으로 진입한다.
4. 문제 세부 화면은 **객관식 전체 문제 + 본인이 선택한 보기 + 정답 + 해설을 한 번에** 표시한다.
5. 오답노트에서 제공하는 학생 액션은 **`메모 추가` · `책갈피` 두 가지**다.
6. **오답을 다시 푼다고 해서 정답으로 전환되지 않는다** — 한 번 누적된 오답은 그대로 보존된다 (복습 기록 보호).
7. **오답노트는 매니저뷰에 노출하지 않는다** — 학생 자기학습 영역으로 보호한다.

### 3.2 보충 퀴즈

1. 보충 퀴즈는 **오답노트와 분리된 별도 탭**에서 학생이 직접 생성해 푸는 형태다. 오답노트 내부에서 직접 푸는 액션은 두지 않는다.
2. 보충 퀴즈는 **학생이 직접 생성**한다 (날짜·오답중심·문제 수 옵션).
3. **별도의 상시 한도는 두지 않는다**. 단 어뷰징 방지를 위해 **학생 1인 하루 최대 100문제까지** 생성 가능하다.
4. 하루 100문제 한도를 초과해 생성을 시도하면 인라인 에러를 노출한다 — **"오늘 한도를 넘었습니다"**.
5. **매니저는 보충 퀴즈를 생성·발행하지 않는다**. 매니저뷰에서는 학생별·날짜별 사용수 히트맵 통계만 확인할 수 있다 (하루 10건이 가장 붉은 색).
6. 보충 퀴즈는 **별도 결과 영역**에 누적된다. 오답노트(`§3.1`)의 정답 상태에 영향을 주지 않는다.
7. **매니저 보충 퀴즈 사용현황 히트맵·셀 클릭 상세는 `풀이가 완료된(제출된)` 보충 세트만 집계한다** (v1.8) — 학생이 출제(생성)만 하고 풀지 않은 세트는 히트맵 강도·셀 클릭 리스트 어디에도 포함하지 않으며, 집계 기준일은 **학생이 그 세트를 푼 날짜**다. (상세 표·근거: POL-CR-03 §2.1)

---

## 4. 실습

1. 메뉴명은 **`실습`**으로 통일한다. `AI 실습` 표기는 사용하지 않는다.
2. **AI 자동 생성·자동 채점은 도입하지 않는다**. 강사가 직접 등록한 실습 문제만 노출한다.
3. **하루에 여러 실습 문제 발행이 가능**하다 (날짜별 리스트 → 그날 문제 리스트).
4. **제출 마감 시간을 두지 않는다**. 결과를 내지 못한 실습도 이후에 다시 제출할 수 있다.
5. 학생 제출 항목 — **텍스트 필수**, 파일·이미지·URL은 선택.
6. 매니저 채점은 **점수·코멘트 모두 옵션**이다. 채점하지 않은 제출은 학생 화면에서 `-`로 표시한다.
7. **매니저에게 채점 의무·리마인더를 두지 않는다**. 제출 유무 자동 집계만 표시한다.
8. 매니저 채점 뷰는 **학생×문제 표**로 구성한다 (제출자 색상 / 미제출 회색).
9. 매니저는 실습 발행 시 **캠프파이어 공지 옵션**으로 채널·메시지를 함께 등록할 수 있다.
10. 발행 후 매니저는 **텍스트만 수정 가능**하며, 수정 시 최근 수정 시간과 학생 카드에 `수정됨` 배지를 노출한다.

---

## 5. TIL

1. 페이지명은 **`나의 TIL`**, 부제는 "매일 배운점을 정리해보세요"로 표기한다.
2. 작성 폼은 **마크다운 본문 단일 에디터**로 구성한다. 빈 본문에는 템플릿 placeholder를 자동 삽입한다 — `# 오늘 배운것` · `# 막힌것` · `# 내일 할 것`.
3. 작성 폼 우측에 **오늘의 컨텍스트 3종(AI 노트 · 오답노트 · 실습)을 읽기 전용**으로 노출한다.
4. 작성 중 **자동 저장** — `임시` 상태로 진입한다.
5. `게시하기` 시 **`작성` 상태로 전환**하고 히트맵을 즉시 갱신한다.
6. **게시 이후에도 자유 수정이 가능**하다. 게시일 잠금을 두지 않는다. 최근 수정 시각만 로그로 남긴다.
7. 본인 페이지에는 **활동 기록 히트맵 + 작성 카드 리스트**를 노출한다 (3색: 미작성·임시·작성).
8. 매니저뷰는 **학생×날짜 히트맵**(3색 + 범례)으로 표시하며, **셀 클릭 시 본문 슬라이드 패널**을 연다.
9. **위험군 별도 라벨을 두지 않는다** — 히트맵 회색으로 자연스럽게 식별한다.
10. 마크다운 복사 시 **메타데이터를 포함하지 않고 본문만 복사**한다.

---

## 6. 프로젝트

1. 운영 회차는 **기초 · 심화 · 파이널** (기본 3종) + **`기타 프로젝트`** (매니저 선택)로 구성한다.
2. 각 회차는 **제출 기간 + 피드백 기간을 분리**해 설정한다.
3. 회차 등록 필수 항목 — **제목 · 설명 · 프로젝트 URL · 발표자료 · 시연 영상 URL**.
4. 학생 제출은 **회차당 1건**으로 제한한다. **마감 전까지는 수정 가능**하다.
5. 제출 폼 필수 항목 — 제목 · 한 줄 소개 · 설명(마크다운) · 메인 이미지 · 프로젝트 URL(다중) · 시연 영상 URL.
6. 시연 영상 URL은 **YouTube / Vimeo만 허용**한다.
7. **제출 후 해당 수업 내 게시판에 자동 공개**된다 — 다른 학생의 제출물을 열람할 수 있다.
8. **피드백(매니저 코멘트)은 본인만 확인 가능**하다 — 다른 학생에게 노출하지 않는다.
9. **외부 발행은 기본 OFF**다. 학생이 명시적으로 `발행` 토글을 ON 해야 외부 발행 링크가 생성된다.
10. 외부 발행 URL은 권한 검사 후 노출하며, **비공개 전환 시 즉시 차단**한다.
11. 매니저는 모더레이션 권한으로 `비공개 전환` · `우수 프로젝트 마크`를 사용할 수 있다.
12. **매니저는 전체 단계 `예정`(= 제출 시작 전, 발행 전)인 회차를 삭제할 수 있다** (v1.4) — 확인 모달 후 삭제. **제출 시작(발행) 이후에는 삭제 불가**하며 삭제 액션을 노출하지 않는다.
13. 공개된 **제출 프로젝트 리스트는 본인 제출본을 1페이지 맨 앞에 고정**한다 (v1.5) — 본인 제출본은 페이지네이션에서 제외하여 **2페이지부터는 보이지 않으며**, 다른 학생 제출본만 페이지당 N건으로 나눠 탐색한다.
14. 매니저뷰의 **인원 통계는 중도포기자를 제외한 재적생 기준**이다 (v1.6) — **전체 학생수(분모)·제출률 분자 = 재적생**이며 전체 인원 옆에 **`중도포기자 제외`** 안내를 노출한다. **미제출 인원은 재적생 중 미제출자만** 세고(중도포기자 미포함), 단 **이미 제출한 프로젝트는 중도포기 여부와 무관하게 삭제·비활성화하지 않고 보존**한다(`중도포기` 배지로만 식별).

---

## 7. 알림 (이번 라운드 = 슬랙 live-class-alert 단일 · 2개 실패 이벤트만)

> **스코프 축소(v1.3, 2026-06-09)**: 본 라운드 알림은 **슬랙 `live-class-alert` 1채널**, **AI 노트 생성 실패 · 학습퀴즈 생성 실패 2개 이벤트만** 구현한다. 캠프파이어 운영/매니저 알림·학생 개별 DM과 그 외 모든 알림 이벤트는 **차기 범위로 보류**한다(`§7.3`). 이전 4채널 고정 설계 전문은 아카이브 스냅샷(`policies/archive/POL-CR-01-classroom-core-v1.2-snapshot-2026-06-09.md` §7·PRD §8)에 보존한다.

### 7.1 본 라운드 구현 범위 (슬랙 live-class-alert 단일)

1. 알림은 **슬랙 `live-class-alert` 채널로만** 발송한다. 캠프파이어 알림·학생 DM·인앱 인박스·토스트는 본 라운드에서 사용하지 않는다.
2. 발송 대상 이벤트는 **아래 2개의 생성 실패 알림뿐**이다. 둘 다 **자동 발송**이며 **학생에게 노출하지 않는다**(멋사 내부 매니저 수신).
   - **AI 노트 생성 실패(미생성 오류)** → 슬랙 `live-class-alert`
   - **학습퀴즈 생성 실패(미생성 오류)** → 슬랙 `live-class-alert`
3. 슬랙 `live-class-alert`의 수신 대상은 **멋사 내부 매니저**다 (캠프파이어 도메인 권한 기준 — 클래스룸 학생/매니저 2종과 구분, `§1` 참조).

### 7.2 슬랙 알림 메시지 요건

1. 메시지에는 **수업명 · 이벤트 종류(AI 노트 / 학습퀴즈) · 대상 일자 · 발생 시각**을 포함한다.
2. 생성 실패는 그날 콘텐츠 미발행과 동일하게 처리하며(학생 비노출), 매니저는 매니저뷰의 **즉시 재생성·수동 발행** 동선으로 복구한다(`§2.6` 학습퀴즈 / AI 노트 운영 spec).

### 7.3 차기 범위로 보류 (이번 라운드 미구현)

아래 알림은 **설계만 보존**하고 이번 라운드에서는 구현하지 않는다. 전체 매트릭스 설계는 아카이브 스냅샷 §7·PRD §8을 참조한다.

| 보류 항목 (채널) | 기존 설계 (차기 검토) |
|---|---|
| 캠프파이어 "운영 알림" | 학습 일정·발행 공지(AI 노트 생성 완료·퀴즈 발행·실습 발행 등) |
| 캠프파이어 "매니저 알림" | 운영 신호(TIL 3일 연속 미작성·프로젝트 제출 등) |
| 학생 개별 DM (캠프파이어) | 실습 채점 완료·프로젝트 외부 공개 제한 사유 등 |
| 슬랙 — 프로젝트 외부 발행 추가 알림 | 외부 발행 ON 시 `live-class-alert` 추가 발송 |
| 매니저 수신 채널 선택 옵션 | 4채널 중 본인 수신 채널 선택(기본 캠프파이어 매니저 알림) |
| 퀴즈 미응시 / 보충 퀴즈 발행 알림 | (기존에도 미발송 결정 — 명단·직접 생성으로 갈음) |

---

## 8. 첨부파일

1. **이미지·파일 1건당 30MB까지** 첨부 가능하다 (실습 · 프로젝트 공통).
2. 시연 영상은 **YouTube / Vimeo URL만** 등록 가능하다.
3. 첨부 URL은 형식 검증(스킴·도메인)을 통과해야 한다.
4. 용량 초과 시 표준 문구: **"이미지·파일은 30MB까지 첨부 가능해요"**.
5. URL 형식 오류 시 인라인 에러: **"잘못된 URL 입니다"** 또는 도메인별 안내(`"YouTube 또는 Vimeo 링크만 등록 가능해요"`).
6. 프로젝트 메인 이미지 용량 초과 시 **자동 리사이즈를 시도**하고, 실패 시 에러로 처리한다.

---

## 9. 본 라운드 범위 외 (비정책)

- **마이페이지 통합 위젯 — 별도로 제공하지 않는다**. 향후에도 본 정책 범위 내에서는 만들지 않는다.
- **팀 프로젝트 별도 처리 X** — 팀 단위 제출 기능을 두지 않고, **팀 프로젝트라도 학생 개개인이 모두 제출**한다 (§6 프로젝트 정책 4번 "회차당 1건" 그대로 적용).
- 인앱 인박스/토스트, 이메일·SMS 알림.
- 외부 발행 도메인 커스터마이즈.
- 다른 학생 TIL 공개 열람, peer review(동료 평가).
- 위험군 별도 라벨/임계치 (Admin 대시보드 고도화에서 진행 예정).
- 매니저 채점 의무·기한 (운영 정책 별도 검토).

---

## 10. 변경 이력

| 버전 | 날짜 | 변경 |
|---|---|---|
| 1.2 | 2026-05-27 | PRD v1.2 §9 정책 확정 사항 단일 파일로 통합 — 권한 2종, 퀴즈 자동 발행/오답·보충, 실습 마감 없음·30MB, TIL 자유 수정, 프로젝트 게시판 공개·외부 발행 기본 OFF, 알림 4채널 고정 |
| **1.3** | **2026-06-09** | **§7 알림 스코프 축소** — 4채널 고정 → **슬랙 `live-class-alert` 단일 채널**, 발송 이벤트를 **AI 노트 생성 실패 · 학습퀴즈 생성 실패 2건만**으로 제한(둘 다 자동·학생 비노출). 캠프파이어 운영/매니저 알림·학생 DM·기타 모든 이벤트(퀴즈 발행 안내·실습 발행/채점 DM·TIL 3일 미작성·프로젝트 제출/외부공개·매니저 채널 선택)는 §7.3 차기 범위로 보류. 직전 4채널 설계는 `policies/archive/POL-CR-01-classroom-core-v1.2-snapshot-2026-06-09.md`에 스냅샷 보존 |
| **1.4** | **2026-06-10** | **운영 정책 3건 추가(PRD v1.9·POL-CR-03 v1.9 정합)** — ① §2.10 **학생뷰 소요시간 전면 비표시**(응시 중·결과·과거 내역·보충 결과, 소요시간은 매니저뷰 진단 전용). ② §2.11 **퀴즈 발행 폼 빈 칸 검증**(`임시저장`·`예약발행`·`즉시발행` 모두 빈 필드 시 인라인 에러 + 차단). ③ §6.12 **`예정`(제출 시작 전) 프로젝트 회차 매니저 삭제 가능** — 발행(제출 시작) 이후 삭제 불가 |
| **1.5** | **2026-06-10** | **신규 기능 2건 추가(PRD v1.10·POL-CR-03 v1.10 정합)** — ① §2.12 **발행본 `퀴즈 풀이`(수업용)**: 강사가 학생과 함께 한 문제씩 풀고 정답·해설 즉시 공개, 채점·집계·오답 누적 없음(매니저뷰 전용). ② §6.13 **제출 프로젝트 리스트 본인 제출본 1페이지 맨 앞 고정 + 페이지네이션** — 2페이지부터 본인 제출본 비노출 |
| **1.6** | **2026-06-11** | **프로젝트 매니저뷰 중도포기자 집계 규칙 추가(PRD v1.11·POL-CR-03 v1.11 정합, §6.14)** — 인원 통계 분모·제출률 분자=재적생(중도포기자 제외)+`중도포기자 제외` 안내, 미제출=재적생 중 미제출만, 제출본은 중도포기와 무관하게 삭제·비활성화 없이 보존 |
| **1.7** | **2026-06-15** | **학습퀴즈 매니저뷰 중도포기자 집계 규칙 추가(PRD v1.12·POL-CR-03 v1.12 정합, §2.13)** — 발행본 상세(결과 요약·학생별 점수·미응시 명단)·발행 퀴즈 리스트의 인원 집계를 **`퀴즈 생성 시점` 기준 재적생**만으로 한정. 생성 후 중도포기자=점수 보존(`중도포기` 배지), 생성 전 중도포기자=제외, 발행본 상세 상단 `퀴즈 생성 시점 기준 재적생` 안내+출결현황 가이드. 보충 사용현황 히트맵은 누적 뷰라 제외 |
| **1.8** | **2026-06-16** | **매니저 보충 히트맵 — 풀이 완료 세트만 집계 명문화(PRD v1.13·POL-CR-03 v1.13 정합, §3.2-7)** — 보충 퀴즈 사용현황 히트맵·셀 클릭 상세는 **`풀이가 완료된(제출된)` 보충 세트만** 집계. 출제만 하고 풀지 않은 세트는 미포함, 집계 기준일=푼 날짜. (프로토타입 기동작 정책화, 코드 변경 없음) |
